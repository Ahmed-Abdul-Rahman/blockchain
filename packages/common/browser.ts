/**
 * Browser entry for `@dechat/common` — console logger, no file / worker_threads deps.
 */
export { pickRandom, sampleIndices } from './src/commonUitls';
export { logger } from './src/logger.browser';
export type { Logger } from './src/loggerShared';
